using TMPro;

namespace KT
{
  // Just in case we need to implement behaviour to texts.
  public class TextBox : TextMeshProUGUI
  {
    protected override void Awake ()
    {
      base.Awake();
    }

    // Start is called before the first frame update
    protected override void Start ()
    {
      base.Start();
    }
  }
}